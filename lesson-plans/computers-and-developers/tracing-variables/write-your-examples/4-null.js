'use strict';

/* Null

TODO: create a few variables(null):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// declare a let variable and initialise it with null
let what = null;

// read the value of `what` which is null
console.log(what);

// assign the value undefined to the variable `what`
what = undefined;

// read the value of `what` which now should be undefined
console.log(what);
