'use strict';

/* Numbers

TODO: create a few variables(numbers):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// declare a const variable x and initialise it with a number value 16
const x = 16;

// declare a const variable y and initialise it with a number value 85
const y = 85;

// declare a const variable `addition` and initialise it with the number value 16+85
const addition = x + y;

// read the value of addition (101)
console.log(addition);

// declare a const variable `division` and initialise it with the number value 16/85
const division = x / y;

// read the value of division (0.18823529411764706)
console.log(division);
