'use strict';

/* Undefined

TODO: create a few variables(undefined):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// declare a let variable `what` without initialisation
let what;

// can't read the value, which is now undefined
console.log(what);
