'use strict';

/* Booleans

TODO: create a few variables(boolean):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// declare the variable isCute and initialise it with a boolean value - false
let isCute = false;

// if the variable `isCute` is true
if (isCute) {
  // read the string 'Cat is cute!'
  console.log('Cat is cute!');
} else {
  // if the variable `isCute` is false, read the string "Dog is cute too."
  console.log('Dog is cute too.');
}
